name: Build SQLCipher (Windows MinGW-w64)

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'SQLCipher tag (ex: v4.10.0)'
        required: true
        default: 'v4.10.0'

jobs:
  build-win:
    runs-on: windows-latest

    steps:
      - name: 🔹 Checkout repositório
        uses: actions/checkout@v4

      - name: 🔹 Instalar MSYS2 e dependências
        uses: msys2/setup-msys2@v2
        with:
          update: true
          install: >-
            git
            make
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-openssl
            mingw-w64-x86_64-zlib

      - name: 🔹 Build SQLCipher
        shell: msys2 {0}
        run: |
          echo "🔹 Baixando SQLCipher versão ${{ github.event.inputs.tag }}"
          git clone https://github.com/sqlcipher/sqlcipher.git
          cd sqlcipher
          git checkout ${{ github.event.inputs.tag }}

          echo "🔹 Compilando com Makefile.msc..."
          make -f Makefile.msc \
            USE_AMALGAMATION=1 \
            OPT_FEATURE_FLAGS="-DSQLITE_HAS_CODEC -DSQLCIPHER_CRYPTO_OPENSSL -DSQLITE_TEMP_STORE=2" \
            LTLIBS="-lcrypto -lz"

          echo "🔹 Preparando saída..."
          mkdir -p ../output
          cp sqlite3.exe ../output/ || true
          cp sqlite3.dll ../output/ || true
          cp sqlite3.h ../output/
          cp sqlite3ext.h ../output/

      - name: 🔹 Criar arquivo ZIP com os binários
        run: |
          powershell Compress-Archive -Path output/* -DestinationPath sqlcipher-windows.zip

      - name: 🔹 Publicar artefato
        uses: actions/upload-artifact@v4
        with:
          name: sqlcipher-windows
          path: sqlcipher-windows.zip
