name: Build SQLCipher (Windows Native MinGW)

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'SQLCipher tag (ex: v4.10.0)'
        required: true
        default: 'v4.10.0'

jobs:
  build-win:
    runs-on: windows-latest

    steps:
    - name: 📦 Checkout
      uses: actions/checkout@v4

    - name: 🧰 Instalar MSYS2 + Toolchain
      uses: msys2/setup-msys2@v2
      with:
        update: true
        msystem: MINGW64
        install: >-
          git
          mingw-w64-x86_64-toolchain
          mingw-w64-x86_64-openssl
          make
          zip

    - name: 🔹 Clonar SQLCipher
      shell: pwsh
      run: |
        git clone https://github.com/sqlcipher/sqlcipher.git C:\sqlcipher
        cd C:\sqlcipher
        git checkout ${{ github.event.inputs.tag }}

    - name: ⚙️ Compilar SQLCipher (sem MSYS2 shell)
      shell: pwsh
      run: |
        $env:PATH = "C:\msys64\mingw64\bin;$env:PATH"
        cd C:\sqlcipher

        echo "🔹 Configurando ambiente..."
        copy .\Makefile.msc .\Makefile.windows

        echo "🔹 Compilando..."
        gcc -c sqlite3.c -DSQLITE_HAS_CODEC -DSQLCIPHER_CRYPTO_OPENSSL -DSQLITE_TEMP_STORE=2 -I"C:\msys64\mingw64\include" -L"C:\msys64\mingw64\lib" -lcrypto -lssl -lz

        echo "🔹 Gerando DLL..."
        gcc -shared -o libsqlcipher.dll sqlite3.o -L"C:\msys64\mingw64\lib" -lcrypto -lssl -lz

        echo "🔹 Gerando EXE..."
        gcc -o sqlite3.exe shell.c libsqlcipher.dll -L"C:\msys64\mingw64\lib" -lcrypto -lssl -lz

    - name: 📦 Empacotar binários
      shell: pwsh
      run: |
        mkdir build
        copy C:\sqlcipher\sqlite3.exe build\
        copy C:\sqlcipher\libsqlcipher.dll build\
        copy C:\sqlcipher\sqlite3.h build\
        copy C:\sqlcipher\sqlite3ext.h build\
        cd build
        Compress-Archive -Path * -DestinationPath sqlcipher-windows.zip

    - name: ⬆️ Publicar artefato
      uses: actions/upload-artifact@v4
      with:
        name: sqlcipher-windows
        path: build/sqlcipher-windows.zip
